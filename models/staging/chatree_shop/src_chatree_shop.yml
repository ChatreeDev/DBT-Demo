# ห้ามลืมใส่ version 2

version: 2

sources:
  - name: chatree_shop
    database: raw
    schema: chatree_shop
    tables:
      - name: customers
        columns:
          - name: id
            tests:
              - unique
              - not_null
            
      - name: orders
        columns:
          - name: id
            tests:
              - unique              
              - not_null